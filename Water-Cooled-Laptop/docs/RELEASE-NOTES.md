# REV10 版本说明 [中文]

**语言 / Language: 中文（简体） | [English](RELEASE-NOTES.en.md)**

- 建模版本：REV10。
- 建模完成日期：2026-09-01。
- 项目目录整理日期：2026-09-07。
- 核对来源：`Acrylic_ThreePlates_363x244x3_REV10_2026-09-01.zip` 与本地最终导出文件。
- 本次整理保留三块板的原始 F3D、STEP、DXF；没有重新建模或修改孔位。

## 最新修改

相较上一版，SolidBackPlate 顶边新增 5 × 5 mm 缺口，右侧边缘距右边 180 mm。RAMSidePlate 和 ThirdPlate 沿用已经完成的最终导出。

三块板均为 363 × 244 × 3 mm、四角 R3；M3 通孔数量分别为 8、12、6，各加工 1 块。

## 公开目录整理

- 按 `images/`、`CAD/`、`electronics/`、`docs/` 组织。
- 保留六张已有实物照片及三个零件的原始 CAD 预览。
- 加入 Fusion 生成脚本和理论几何检查工具。
- 参数报告的 `files` 字段改为仓库根目录相对路径，几何数据保持原值。
- 生成脚本与原工作区保持一致；从本仓库运行后，输出在 `CAD/output/`。
- 旧版本、缓存文件和本机运行输出不纳入公开目录。

本次核对针对现有文件与已记录参数；未重新启动 Fusion 执行建模，也未新增实物装配或性能测试。


## 本次文件核验

- 原 REV10 压缩包共 17 项，完整性检查通过，与当前最终导出的对应文件 SHA-256 全部一致。
- 三份 DXF 均为 mm；解析的圆孔数量、R3 外角和背板缺口与说明一致。
- 三份 STEP 各包含一个实体，文件内部长度单位为 cm，外形换算后为 363 × 244 × 3 mm。
- 三个生成脚本、三个 manifest 和几何检查工具按原件复制；Python 语法与 manifest JSON 检查通过。
- F3D 按原文件附带，本轮未在 Fusion 内重新打开。
